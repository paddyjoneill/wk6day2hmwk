const assert = require('assert');
const Decorator = require('../decorator.js')

describe('decorator', function() {

  let decorator

beforeEach(function() {

  decorator = new Decorator()
}
  it('start with an empty paint stock')

  it('should be able to add a can of paint to paint stock')

  it('Should be able to calculate the total litres of paint it has in stock')

  it('It should be able to calculate if it has enough paint to paint room')

  it('Should be able to paint the room if it has enough paint')
})
